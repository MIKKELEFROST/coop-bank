node_modules/
.DS_Store

# dist/ holds rendered output. Only the actual deliverables are tracked;
# everything else (frame sequences, scratch renders, check sheets) is
# reproducible from `npm run render`.
dist/*
!dist/voiceover-manus.txt
!dist/voiceover-timing.json
!dist/voiceover-da.srt
!dist/storyboard-contact-sheet.png
!dist/coop-bank-ai-agenter-preview.mp4
!dist/coop-bank-ai-agenter-retimet.mp4
!dist/coop-bank-ai-agenter-retimet-2997.mp4
!dist/retime-map.json
